# valence-backend

Pure schema, iter, and deletion helpers used by `valence-app` server functions.
Kept free of Leptos/UI deps so Layer 1 verification can run when the host
product UI graph is unavailable.

Crate-root rustdoc owns Organized-by-task, Owns / does not own, Concern → API,
and the Examples. See [`docs/VERIFICATION.md`](../docs/VERIFICATION.md).
