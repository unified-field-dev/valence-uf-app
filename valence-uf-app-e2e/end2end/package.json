{
  "name": "valence-uf-app-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "wait-server": "wait-on -t 300000 -i 500 -l http-get://localhost:3130/health",
    "test": "npm run wait-server && npx playwright test --workers=1",
    "test:headed": "npm run wait-server && PW_HEADED=1 npx playwright test --workers=1 --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "wait-on": "^7.2.0"
  }
}
